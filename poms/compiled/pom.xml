<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.ops4j.pax.web.pax-web-build</groupId>
    <artifactId>bundles</artifactId>
    <version>0.2.1-SNAPSHOT</version>
  </parent>

  <groupId>org.ops4j.pax.web.pax-web-build</groupId>
  <artifactId>compiled</artifactId>
  <version>0.2.1-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>OPS4j Pax Web Build Compiled</name>
  <description>
    Configure OSGi bundles compiled from source
  </description>

  <properties>
    <bundle.location>${project.build.directory}/${project.build.finalName}.jar</bundle.location>
  </properties>

  <!--
    typical OSGi development dependencies
  -->
  <dependencies>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>osgi_R4_core</artifactId>
      <version>1.0</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>osgi_R4_compendium</artifactId>
      <version>1.0</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <!--
      source specific plugin configuration
    -->
    <pluginManagement>
      <plugins>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <source>1.5</source>
            <target>1.5</target>
            <debug>true</debug>
          </configuration>
        </plugin>
        <plugin>
          <artifactId>maven-dependency-plugin</artifactId>
          <executions>
            <execution>
              <id>embed-jar</id>
              <phase>generate-resources</phase>
              <goals>
                <goal>copy-dependencies</goal>
              </goals>
              <configuration>
                <outputDirectory>${project.build.directory}</outputDirectory>
                <excludeTransitive>true</excludeTransitive>
                <excludeScope>provided</excludeScope>
                <stripVersion>true</stripVersion>
              </configuration>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.ops4j.pax.maven</groupId>
          <artifactId>maven-bundle-plugin</artifactId>
          <extensions>true</extensions>
          <configuration>
            <instructions>
              <Bundle-Version>${project.version}</Bundle-Version>
              <Bundle-SymbolicName>${bundle.name}</Bundle-SymbolicName>
              <!--
                assume public classes are in the top package, and private classes are under internal
              -->
              <Private-Package>${bundle.package}.internal</Private-Package>
              <Export-Package>!${bundle.package}.internal,${bundle.package}.*;version="${project.version}"
              </Export-Package>
              <!--
                load project specific settings
              -->
              <_include>-target/classes/META-INF/details.bnd</_include>
            </instructions>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.ops4j.pax.construct</groupId>
          <artifactId>maven-pax-plugin</artifactId>
          <executions>
            <execution>
              <id>clean-project</id>
              <phase>clean</phase>
              <goals>
                <goal>clean</goal>
              </goals>
            </execution>
            <execution>
              <id>backup-metadata</id>
              <phase>generate-resources</phase>
              <goals>
                <goal>backup</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

</project>
